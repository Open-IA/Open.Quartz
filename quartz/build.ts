import sourceMapSupport from "source-map-support";
import { options } from "./util/sourcemap";
sourceMapSupport.install(options);

async function buildQuartz(argv) {

}

export default async () => {

}